快捷入口页面当前结构正确，不再改变架构。

继续优化：

1. 删除顶部“新增快捷入口请使用首页末尾+按钮”的大提示卡。

改为普通辅助文字。

2. 删除标题重复。

保持：
Tab：快捷入口

页面标题：
显示设置


3. 开关区域改为系统设置样式：

左侧标题：
显示首页快捷入口

右侧开关。


4. 桌面端数量、移动端数量改成两个独立设置卡片。

保持玻璃风格。


5. 排序方式改成：

[自动排序][手动排序]

使用Tab/Segmented Control样式。

说明文字紧贴排序选项。


6. 保持当前：
- 毛玻璃
- 深色
- 圆角
- 青绿色强调


不要增加网站管理。

修正快捷入口设置逻辑。

项目当前为纯静态部署，本地缓存模式。

PC和手机不是同一配置源，因此不要在PC设置页面显示手机端配置。

修改：

PC设备：

只显示：
- 显示快捷入口开关
- 桌面端显示数量
- 排序方式


移动端：

只显示：
- 显示快捷入口开关
- 移动端显示数量
- 排序方式


根据当前设备自动判断展示对应设置。

不要显示另一个设备的配置，避免用户误认为会同步。

所有设置只保存当前设备localStorage。

提示文字：
“设置仅保存在当前设备”
目标：
类似macOS设置中的“Dock设置”，而不是后台管理页面。