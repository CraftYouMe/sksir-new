请基于当前源码进行性能优化，不改变UI和功能。

目标：
Lighthouse性能从86提升到95左右。

重点：

1. 优化图片加载：
- 所有网站favicon不要直接加载ico
- 自动转换为WebP
- favicon尺寸限制32x32或64x64
- 建立favicon缓存机制
- 获取失败提供fallback

2. 优化背景：
- 生成响应式背景图片
- 移动端加载小尺寸版本
- 背景延迟加载，避免阻塞首屏

3. 优化首屏：
- 设置页面相关JS改为动态加载
- 非首页必要资源延迟加载

4. 优化字体：
- font-display设置swap
- 检查字体是否可以子集化

5. 优化缓存：
- 静态资源增加长期缓存策略
- OSS资源增加immutable缓存

6. 不允许：
- 大规模重构HTML
- 修改UI布局
- 修改数据结构
- 删除已有功能

每次修改后提供：
- 修改文件列表
- 修改原因
- Lighthouse预期提升