# Phase 7 跨设备验收表

代码版本：`2026.07.26.27`
执行状态：待真实设备填写

测试前先记录：

- 测试日期：
- 设备与型号：
- 操作系统版本：
- 浏览器与版本：
- 网络环境：
- 是否首次访问：
- 是否开启减少动态效果：
- 是否使用 `perf-lite`：

每项填写“通过 / 失败 / 未测”。失败时附截图、复现步骤和控制台错误。

## PC

建议视口：1440 × 900、1280 × 720。

| 项目 | 结果 | 备注 |
|---|---|---|
| 首屏时间、日期、搜索框和壁纸正常出现 | 通过 | 2026-07-26，内置 Chromium，1440 × 900、1280 × 720；远程壁纸失败时深色回退正常 |
| 首屏未提前请求收藏数据和状态检测 | 未测 | |
| 搜索框进入和退出无跳动 | 通过 | 聚焦与 Escape 退出后位置、状态恢复正常 |
| placeholder 在搜索态隐藏，退出后恢复 | 通过 | 回归发现 Chromium 未隐藏，已在 `2026.07.26.26` 修复并复测 |
| 本地收藏建议和必应联想可用 | 通过 | `GitHub` 返回本地书签及必应建议，共 8 项 |
| 离线或远程失败时本地结果保留 | 未测 | |
| 搜索引擎切换、Alt + 1～4 正常 | 通过 | 面板正常展开；非编辑态 Alt + 1 切换到百度 |
| 快捷入口显示、点击统计和拖动排序正常 | 未测 | 显示、点击统计和自动排序通过；两次自动指针拖动未产生重排，需实体鼠标复核 |
| 收藏首次打开后才加载数据和图标 | 通过 | 首次打开后渲染收藏中心与站点卡片 |
| 收藏分组、分类、搜索和空结果正常 | 通过 | 2026-07-27 复测：切换“工具”分组后分类更新；无匹配搜索显示空状态，清空后正常恢复 |
| 设置分组、壁纸、性能模式和搜索引擎编辑正常 | 通过 | 2026-07-27 复测：每日必应壁纸选择与说明同步，随后恢复随机壁纸；轻量动效即时应用 `perf-lite`，随后恢复自动模式 |
| 更新检查和页脚版本正常 | 通过 | 页脚显示 `v2026.07.26.26` |
| 控制台无未处理异常 | 通过 | 本轮错误与警告日志为空 |

性能记录：

- 首屏核心结构可见时间：
- `window.__sksirFirstScreenMs`：
- 壁纸等待结果 `window.__sksirBootWallpaperWait`：
- 最大布局偏移：
- 超过 50 ms 的长任务数量：

补充记录：1280 × 720 下文档尺寸与视口一致（1280 × 720），无页面溢出。当前浏览器控制隔离层不暴露页面自定义 `window` 性能字段和 Performance Timeline，因此未伪造上述四项数据；远程壁纸失败时，完整 `load` 墙钟时间约 2759 ms，只作为故障环境参考，不等同于首屏核心结构可见时间。

2026-07-27 在 1440 × 900 再次尝试读取首屏自定义字段、导航条目、布局偏移和长任务，隔离层仍全部返回不可用；本轮控制台错误与警告为空。

## iPhone Safari

建议至少覆盖 390 × 844 和 375 × 667。

聚焦前后分别在远程调试控制台采集：

```js
({
  innerHeight: window.innerHeight,
  scrollY: window.scrollY,
  visualViewportHeight: window.visualViewport && window.visualViewport.height,
  visualViewportOffsetTop: window.visualViewport && window.visualViewport.offsetTop,
  activeElement: document.activeElement && document.activeElement.className,
  standalone: window.navigator.standalone === true
})
```

| 项目 | 结果 | 备注 |
|---|---|---|
| 初次打开壁纸铺满，无灰黑区域 | 未测 | |
| 地址栏伸缩后背景仍铺满 | 未测 | |
| 搜索聚焦不发生异常整页跳动 | 未测 | |
| 输入框字号无自动缩放 | 未测 | |
| 键盘关闭后时间和搜索区域恢复 | 未测 | |
| 建议列表位于搜索框下方且可滚动 | 未测 | |
| placeholder 行为正确 | 未测 | |
| 收藏和设置关闭按钮可触达 | 未测 | |
| Toast、页脚和设置按钮尊重安全区 | 未测 | |
| 分类横向滚动顺畅 | 未测 | |

禁止在验收中临时加入整页反向位移、反复 `scrollTo`、滚动锁或键盘中间高度写入来掩盖问题。

## iPhone standalone

| 项目 | 结果 | 备注 |
|---|---|---|
| `navigator.standalone` 为 `true` | 未测 | |
| 顶部和底部背景铺满安全区 | 未测 | |
| `--ios-standalone-bg-height` 更新正常 | 未测 | |
| 搜索键盘打开和关闭后背景不闪黑 | 未测 | |
| 收藏、设置、Toast 和页脚位置正常 | 未测 | |

## 安卓

建议视口：412 × 915，并补充一台低性能设备。

| 项目 | 结果 | 备注 |
|---|---|---|
| 输入框聚焦不异常缩放 | 未测 | |
| 建议列表定位正确 | 未测 | |
| 搜索引擎面板为两列并位于输入框下方 | 未测 | |
| 分类横向滚动顺畅 | 未测 | |
| 收藏卡片密度与文字截断正常 | 未测 | |
| 系统返回键或关闭操作行为正常 | 未测 | |
| `perf-lite` 动画和模糊降级正常 | 未测 | |
| 低性能设备无明显连续掉帧 | 未测 | |

## 故障模拟

| 场景 | 预期 | 结果 |
|---|---|---|
| 浏览器离线 | 时间、搜索框和本地收藏结果仍可用，不请求远程联想 | 未测 |
| 远程联想超时 | 本地收藏结果保留 | 未测 |
| 壁纸 URL 失败 | 页面核心内容可见，有既有背景回退 | 未测 |
| 单个收藏图标失败 | 使用本地图标回退，不影响其他卡片 | 未测 |
| 收藏数据加载失败 | 首页和搜索基本功能不崩溃 | 未测 |
| 状态接口失败 | 收藏仍可浏览，状态点失败不阻断交互 | 未测 |
| localStorage 数据损坏 | 快捷入口回退到安全默认值 | 未测 |
| 搜索引擎 Cookie 损坏 | 回退到内置搜索引擎列表 | 未测 |

## 发布判定

只有以下条件全部满足后才能把 Phase 7 标记为完成：

- PC 核心功能全部通过；
- iPhone Safari 与 standalone 核心场景通过；
- 安卓核心场景通过；
- 无未解释的数据丢失；
- 性能数据已记录且无明显首屏退化；
- `node scripts\check.js` 与 `git diff --check` 通过；
- 版本文件与页脚一致；
- 所有失败项已有修复或明确的发布阻塞结论。
