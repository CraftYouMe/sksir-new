# Phase 6 JavaScript 职责拆分

## 第一轮 - 2026-07-26 - `2026.07.26.3`

状态：部分完成

### 已迁移

- `js/storage.js`
  - localStorage 安全读取、JSON 解析、写入和删除；
  - 损坏 JSON 自动清理并回退默认值；
  - 已接入快捷入口、最近收藏和性能模式。
- `js/bookmarks.js`
  - 收藏内搜索；
  - 分组/分类组合过滤；
  - 空结果；
  - 收藏关闭按钮；
  - 继续随收藏数据和渲染器按需加载。

### 体积与 jQuery

| 文件 | 大小 | 行数 | `$(` 使用 |
|---|---:|---:|---:|
| `main.js` | 32,440 bytes | 950 | 32 |
| `set.js` | 74,974 bytes | 2,198 | 232 |
| `storage.js` | 1,025 bytes | 55 | 0 |
| `bookmarks.js` | 1,739 bytes | 44 | 0 |

基线为 `main.js` 33,661 bytes / 35 处，`set.js` 75,291 bytes / 232 处。本轮没有删除 jQuery，现有依赖继续保留。

### 加载策略

- `storage.js` 在 `set.js` 前以 defer 加载，作为首屏配置基础模块。
- `bookmarks.js` 不进入首屏脚本链，用户打开收藏时才在 `nav-render.js` 后加载。
- 收藏数据、远程图标和状态资源的按需边界保持。

### 后续

- 迁移壁纸职责；
- 迁移快捷入口职责；
- 迁移设置面板职责；
- 最后再评估搜索模块和 jQuery 删除条件。

## 第二轮 - 2026-07-26 - `2026.07.26.7`

状态：部分完成

- 新增 `js/settings.js`，负责设置中心分组组织、本机数据导出和确认重置。
- 新模块使用原生事件委托，不包含 jQuery。
- `set.js` 从 74,974 bytes / 2,198 行缩减至 72,649 bytes / 2,149 行。
- `set.js` jQuery 使用从 232 降至 231 处。
- 设置模块位于 `set.js` 后 defer 加载，继续复用现有 Cookie 实现与存储键。
- 下一轮继续迁移壁纸和快捷入口；当前仍不满足删除 jQuery 条件。

## 第三轮 - 2026-07-26 - `2026.07.26.8`

状态：部分完成

- 新增 `js/wallpaper.js`，负责壁纸类型切换、说明文字、自定义地址显隐与保存校验。
- 新模块使用原生事件委托，不包含 jQuery；继续调用 `set.js` 中现有的 `getBgImg` / `setBgImg`，保持 Cookie 格式和壁纸应用行为。
- 首屏早期壁纸读取、`--ios-wallpaper-image` 同图兜底和 iOS 高度变量链路均未迁移，避免影响首屏与 Safari 背景。
- `set.js` 从 72,649 bytes / 2,149 行 / 231 处 jQuery 缩减至 70,688 bytes / 2,095 行 / 218 处 jQuery。
- `wallpaper.js` 为 2,300 bytes / 62 行 / 0 处 jQuery。
- 下一轮继续迁移快捷入口；当前仍不满足删除 jQuery 条件。

## 第四轮 - 2026-07-26 - `2026.07.26.9`

状态：部分完成

- 新增 `js/quick-launch.js`，负责快捷入口开关、数量选择、收藏添加、手动添加、删除、恢复自动排序及跨标签页同步事件。
- 收藏卡片点击计数迁入新模块，继续复用原有最近访问记录与排序数据。
- 新模块使用原生事件委托，不包含 jQuery；localStorage 键、数据结构和最多 24 个自定义入口的限制保持不变。
- 首页快捷入口渲染与 Pointer Events 拖动实现仍保留在 `set.js`，桌面拖动阈值、手机触控及拖动后点击抑制逻辑未改写。
- `set.js` 从 70,688 bytes / 2,095 行 / 218 处 jQuery 缩减至 66,464 bytes / 2,014 行 / 201 处 jQuery。
- `quick-launch.js` 为 6,052 bytes / 137 行 / 0 处 jQuery。
- 下一轮迁移快捷入口渲染核心；当前仍不满足删除 jQuery 条件。

## 第五轮 - 2026-07-26 - `2026.07.26.12`

状态：部分完成

- 首页快捷入口 DOM 渲染、图标回退、点击记录、Pointer Events 拖动排序和顺序保存迁入 `js/quick-launch.js`。
- 保持 7px 拖动启动阈值、拖动结束后 400ms 点击抑制、横向位置判定和 pointer capture 行为。
- `set.js` 保留数据清洗、候选项合并与排序服务，并通过窄接口供模块调用；原 `prepareQuickLaunchForBoot` 首屏入口保持。
- `set.js` 当前为 63,245 bytes / 1,927 行 / 199 处 jQuery。
- `quick-launch.js` 当前为 10,086 bytes / 239 行 / 0 处 jQuery。
- 快捷入口可见交互已完成原生化；下一轮评估把数据服务一并迁出，之后进入搜索职责拆分。

## 第六轮 - 2026-07-26 - `2026.07.26.13`

状态：部分完成

- 新增 `js/search-ui.js`，负责搜索建议层尺寸和位置计算、输入交互触发及窗口尺寸变化后的合帧更新。
- 新模块使用原生 DOM 和事件监听，不包含 jQuery；`requestAnimationFrame` 合帧策略保持。
- 桌面与手机端的 inset、搜索框下方间距继续使用上一轮确认的视觉参数。
- 未读取或写入 `visualViewport`，未改变 iOS 键盘、输入框焦点、搜索请求或本地收藏结果逻辑。
- `set.js` 当前为 62,118 bytes / 1,889 行 / 192 处 jQuery。
- `search-ui.js` 为 1,811 bytes / 45 行 / 0 处 jQuery。
- 下一轮继续拆分搜索建议的数据与渲染职责；生产代码仍保留 jQuery。

## 第七轮 - 2026-07-26 - `2026.07.26.14`

状态：部分完成

- 本地收藏候选整理、匹配评分、远程建议去重合并及建议列表 DOM 渲染迁入 `js/search-ui.js`。
- 列表项、图标、辅助说明和网络状态提示改用原生 DOM 创建，不再依赖 jQuery。
- 远程 JSONP 请求和请求序号仍留在 `set.js`；成功、离线及失败状态均把当前本地结果传给同一个渲染器。
- 保持本地结果最多 4 条、合并后最多 8 条、名称精确/前缀/全文匹配优先级。
- `set.js` 当前为 59,089 bytes / 1,817 行 / 186 处 jQuery。
- `search-ui.js` 当前为 6,113 bytes / 154 行 / 0 处 jQuery。
- 下一轮迁移搜索建议状态控制和远程请求；生产代码仍保留 jQuery。

## 第八轮 - 2026-07-26 - `2026.07.26.15`

状态：部分完成

- 搜索建议请求序号、延迟调度、本地/远程状态、面板关闭清理和远程联想请求迁入 `js/search-ui.js`。
- 百度联想从 jQuery JSONP 改为原生动态 script JSONP，保留 3.5 秒超时和 `cb` 回调参数。
- 离线时不创建远程请求；新输入、关闭搜索或超时会清理旧 script、定时器和全局回调。
- 请求序号继续隔离竞态，旧响应不能覆盖新输入；远程错误只更新状态，本地收藏结果继续保留。
- `set.js` 当前为 55,946 bytes / 1,730 行 / 181 处 jQuery。
- `search-ui.js` 当前为 10,978 bytes / 295 行 / 0 处 jQuery。
- 搜索建议核心已完成原生化；下一轮迁移搜索结果点击和键盘选择交互。

### 联想源调整 - 2026-07-26 - `2026.07.26.16`

- 远程联想由百度切换到必应 `api.bing.com/qsonhs.aspx`。
- 按必应 `AS.Results[].Suggests[].Txt` 结构合并分组并去重。
- 原生 JSONP、离线保护、超时、竞态隔离和本地结果保留策略不变。
